apiVersion: v1
kind: Service
metadata:
  name: gitea-mcp
  namespace: infra-mcp
spec:
  type: NodePort
  selector:
    app: gitea-mcp
  ports:
    - port: 3401
      targetPort: 3401
      nodePort: 30341
      name: http
