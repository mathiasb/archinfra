apiVersion: v1
kind: Service
metadata:
  name: gitea-http-nodeport
  namespace: gitea
  labels:
    app.kubernetes.io/name: gitea
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: gitea
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30300
