apiVersion: v1
kind: Service
metadata:
  name: supervisor-nodeport
  namespace: supervisor
spec:
  type: NodePort
  selector:
    app: supervisor
  ports:
    - name: mcp
      port: 3200
      targetPort: 3200
      nodePort: 30320
