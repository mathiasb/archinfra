apiVersion: apps/v1
kind: Deployment
metadata:
  name: supervisor
  namespace: supervisor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: supervisor
  template:
    metadata:
      labels:
        app: supervisor
    spec:
      nodeSelector:
        kubernetes.io/hostname: koala
      imagePullSecrets:
        - name: gitea-registry
      containers:
        - name: supervisor
          image: gitea.d-ma.be/mathias/supervisor:0a70d9e972c8d4563b7d9b50c5d08a84a19fd8f5
          ports:
            - containerPort: 3200
          envFrom:
            - secretRef:
                name: supervisor-secrets
          env:
            - name: SUPERVISOR_PORT
              value: "3200"
            - name: LITELLM_BASE_URL
              value: "http://piguard:4000"
            - name: INGEST_BASE_URL
              value: "http://ingestion:3300"
            - name: INGEST_SVC_URL
              value: "http://ingestion:3300"
            - name: LITELLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: supervisor-secrets
                  key: DMABE_LLMAPI_KEY
          volumeMounts:
            - name: brain
              mountPath: /app/brain
      volumes:
        - name: brain
          hostPath:
            path: /var/lib/supervisor/brain
            type: DirectoryOrCreate
