# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  # interval shortened from 1m0s to 10s — see docs/cd-pipeline.md.
  # `flux bootstrap` will reset this if re-run; preserve manually.
  interval: 10s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: https://gitea.d-ma.be/mathias/infra
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./k3s/flux
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
